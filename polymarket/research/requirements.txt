# epsilon_data research library + dashboard — install path for a clean clone.
#   uv pip install -r requirements.txt                    (this repo's .venv is uv-created and
#                                                          has no pip binary of its own)
#   uv sync                                               (equivalent; reads pyproject.toml/uv.lock)
# Plain venv, no uv:
#   python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt   (Windows)
#   python -m venv .venv && .venv/bin/python -m pip install -r requirements.txt       (macOS/Linux)
duckdb>=1.5.2
pandas>=3.0.2
pyarrow>=24.0.0
numpy>=2.4.0
python-dotenv>=1.2.2
streamlit>=1.62.0
plotly>=7.0.0
matplotlib>=3.10.8
# notebooks
ipykernel>=7.2.0
nbformat>=5.10.4
nbconvert>=7.16.0
matplotlib-inline>=0.1.7
# data fetch (scripts/fetch_data.py ONLY — the loader stays boto3-free, it uses DuckDB httpfs)
boto3>=1.35.0
# tests
pytest>=9.0.0
