"""Health check: verifies the ingestion chain is alive — capture files are
being written, no excessive WS gaps, compression is keeping up, and the cloud
sync is current — emitting a status report for monitoring."""
