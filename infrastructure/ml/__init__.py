"""
infrastructure.ml — walk-forward ML harness and feature builders for the crypto
research stack.

Intentionally re-exports nothing: import submodules explicitly, e.g.
`from infrastructure.ml import walk_forward`. (This file exists so the package
is a regular package, not an implicit namespace package — tooling/pytest
resolution was flaky without it.)
"""
