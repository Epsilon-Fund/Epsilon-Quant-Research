---
title: "Inbox EOD — 2026-05-11 cc-crypto"
created: 2026-05-11
archived: 2026-09-09
status: archived — pre-migration vault (May 2026); historical, not current
owner: justin
project: crypto
para: archive
hubs:
  - pre-migration-vault-2026-05
tags:
  - archive
  - pre-migration-vault
  - crypto
---

> **ARCHIVED — PRE-MIGRATION VAULT (May 2026).** Historical record, imported 2026-09-09 from the standalone Cowork vault that predates this repo becoming the brain. Index: [[pre-migration-vault-2026-05]].
> **PARKED (2026-08-25).** The crypto momentum book is live, but this note is a May-2026 snapshot of it, not its current state. The live record is `docs/STRATEGY_REFERENCE.md` and [[TODO]] § Crypto — do not build on this note or quote its numbers as current.

# 2026-05-11 — cc-crypto

Scope: crypto momentum live dashboard (VPS).

## 1. Shipped today (uncommitted — `git status` / `git add` / commit when ready)

- Live tendency trigger in momentum (`Show live tendency` checkbox + WS-driven fragment)
- BB manual-arm picks up live WS close → leverage/stop/TP scale with live price; `🟢 live` badge in the sim badge when WS connected
- Trade-log inputs accept 4dp; momentum displays cap at 3dp / BTC at 2dp
- Backfilled the two missing ENTRY records (ADA + BTC_0510) → 5 closed pairs, P&L tracks reality
- `build_trade_pairs` now matches by `position_id` with orphan-EXIT warnings
- `force_reset_shared_ws()` + `↻ Reconnect WS` button + WS reset on `↻ Refresh data`
- WS subscription loop hardened with per-symbol try/except
- Backtest logic verified: same-bar exit + re-entry is the documented behaviour

## 4. Open questions / blockers

- **WS "stuck 🟡" investigation paused.** Reconnect button + `force_reset_shared_ws()` are in place. When the persistent 🟡 next recurs, grab a terminal slice from around the failure to decide auto-recovery strategy (longer join timeout vs. N-failure auto-reset depends on which error message appears).
- **BB price formatting for BTC still 3dp, not 2dp.** Small follow-up — pass `symbol` to the existing `_fmt_price` calls in bb's Decisions table cells. Trivial when you want it.
- **Binance API read-only sync still on hold** per Justin's call. Recommendation stands: stages 1–2 are pure upside, stage 3 (submit-mode) is the scary one.

## 5. Next 1-3 todos

- Commit the uncommitted work (`git status` / `git add` / commit)
- BB BTC price formatting fix — pass `symbol` to `_fmt_price` in bb's Decisions table cells (trivial)
- Capture terminal output next time WS shows persistent 🟡 → decide auto-recovery strategy
