# STEP H — loader, manual, worked notebook, research dashboard   [DONE · all gates passed]

Written by Claude Code, 2026-08-29. Ran H0 → H1 → H2 → H3 unattended (operator away). Steps E0/E1/E2 accepted; `book` (F) and R2 publish (G) deliberately NOT started. Code committed to branch `alvaro` (commit `bd12630`); nothing under `data/` committed; nothing pushed.

## Headline
The deliverable is **a documented library that a dashboard demonstrates**. `epsilon_data` is that library — 11 public functions, a README manual, an executed worked-examples notebook, and an anti-drift test proving it equals the raw data. The Streamlit terminal (`dashboard/app.py`) is built on it and nothing else. All stop-condition gates are green.

## H0 — units defect fixed
`tokens.parquet` mixed dollars (`median_mid`/`last_mid`) with cents (`median_spread`). Rebuilt in place: **`median_spread` is now DOLLARS** (median 0.0400, max 1.0) and **`median_spread_cents` kept alongside** (median 4.0¢). `mid ± spread/2` is now valid. **All 8 E1 assertions re-run and pass** (30,772 rows; asset_id unique; condition_id ×2; complement symmetric; path unique; no nulls; unresolved=70; universes sum 30,772).

## H1 — `epsilon_data` (the loader) + anti-drift test
Package at `polymarket/research/epsilon_data/` (`config`/`_internal`/`catalog`/`tape`/`__init__`). No side effects on import, no printing, type-hinted, docstringed.

**Final public API:**
`catalog(universe, event, resolved, min_trades, min_days, apply_exclusions=True)` · `events(universe, apply_exclusions=True)` · `search(text, limit=50)` · `resolve(ref)` · `load_l1(ref, start, end)` · `load_trades(ref, start, end)` · `load_pair(condition_id, start, end)` · `load_event(event_slug, start, end)` — the H1 minimum — plus three **documented extensions** the dashboard needed (rather than reading raw paths in a panel, per rule 2): `coverage(universe)`, `reconciliation()`, `activity_by_time(universe)`.

Hard requirements met: **exclusions applied by default** (`apply_exclusions=False` deliberate, documented; an `excluded` column always present so the dashboard shows-and-marks rather than hides); **all ids strings**; **one documented unit per column** post-H0; **data root configurable** (`EPSILON_DATA_ROOT`, default local `research_v1/`); **a UTC `ts` column** exposed alongside raw `timestamp_ms`/`received_ns`; **per-token reads** (l1 is 101 M rows — never whole-table).

**Anti-drift test (`tests/test_loader.py`): PASS.** For a sample of tokens, `load_l1` equals a raw parquet read on row count, first & last row, and an md5 checksum over `mid`. Six more tests pass too (ids-are-strings, resolve round-trip, UTC ts, pair sums to ~1, search finds "fed", reconciliation holds) — **7 passed in 7.4 s**.

## H2 — the manual
`epsilon_data/README.md`: what the library is, the four-level tree, where files live, every table and column with **type/unit/meaning**, the traps (ids-are-strings; `outcome` null for esports team names; `check4_status` categorical and `near_half` is the biggest bucket and not a failure; l1 is deduped not every message; the units), the known gaps, and `identity_status`/`check*` meanings.

`notebooks/epsilon_data_examples.ipynb` (generated by `scripts/build_examples_notebook.py` per repo convention, then executed): **five tasks, errors=0, 4 plots embedded** (task 1 is a table). All five write cleanly through the public API — nothing needed the internals. Findings surfaced by the examples: busiest politics market = "Will Pete Buttigieg win the 2028 US Presidential Election?"; a Fed-July pair mirrors with **median sum exactly 1.000**; the Dem-nominee-2028 NegRisk event (51 markets) — its YES-sum is examined in Step I (the 0.982 quoted in an earlier draft came from an invalid sum-of-medians; corrected there).

## H3 — the dashboard (research terminal)
Streamlit, dark, monospaced figures, one fixed colour language (blue=first/YES, orange=second/NO, green/red=won/lost), a **persistent header** (event, question, both outcomes with win tag, `path`, per-side stats, all five check columns), and **search-first** navigation (search box + busiest-list shortcut, tree as a secondary expander). Built on `epsilon_data` only. **AppTest smoke: landing / Audit / Explore each render with 0 exceptions.**

**Part A — Audit:** reconciliation panel (the two identities shown holding); coverage calendar as a per-date 24-hour strip with three distinct states — **blue active / grey quiet / red true-gap**; distributions (spread, trades, days, mid) with counts; outlier tabs (widest spreads, zero-trade, 40 empty, 14 inverted, 70 unresolved, top trade counts), every row carrying its `path`; activity heatmaps (weekday×hour) per universe; the stale-book cohort view.

**Part B — Explore:** the core market panel (both sides' mid on one axis + spread below + BUY/SELL prints sized by volume); spread-through-the-market's-life (20 slices); resolution path. Empty states say why ("No L1 events … a quiet or unobserved token"), never a blank chart.

## What the data made awkward (and how I resolved it, not worked around it)
- **Aligning two tapes for the YES/NO mirror.** l1 is sparse and event-timestamped with 56%-tied `timestamp_ms`. `load_pair`/`load_event` floor `ts` to 1 s, take the last value per bucket, outer-join and forward-fill — so a mirror or a NegRisk sum is one call. Nothing else was awkward; all five worked examples wrote in a few lines.
- **Coverage needed a quiet-vs-gap distinction the tokens table doesn't carry.** Extended `coverage()` to read the file manifest for *book* presence too, so the calendar can tell a quiet market (book, no trading) from a true gap (no book) — documented, not a panel hack.

## Choices made where the instruction was silent
- **Installed streamlit 1.62 + ipykernel + matplotlib-inline into the research `.venv`** (bootstrapped `pip` via `ensurepip`, since the venv is uv-managed and had no pip). Streamlit is operator-specified for H3; the others are needed to execute/commit the notebook. Charting uses **matplotlib** (already present) — did not add plotly/altair.
- **Loader location** `polymarket/research/epsilon_data/`, importable via `PYTHONPATH=.` or the sys.path bootstrap the notebook/dashboard include; data root defaults to `data/research_v1/`.
- **`resolve(market_slug)`** returns the `outcome_index`-0 side (documented); use `load_pair(condition_id)` for both.
- **Did not commit** `polymarket/research/data_layer/` (the E-step build scripts — they carry machine-specific absolute scratch paths) nor `brain/handoff/` (Cowork's channel; on disk). Commit is the H deliverables only. Say the word and I'll clean+commit the build scripts too.
- **The 30-second acceptance test** is for the operator; I can't perform the human test with the operator away. Navigation is search-first ("fed july" → result → Open → price history), and the AppTest smoke confirms it renders without error. Please run it and tell me if it misses 30 s — I'll fix navigation before anything else.

## What I noticed about the data while building (most valuable)
- **The "esports 2026-07-24 h12 gap" is not a gap.** It has a book snapshot but no price_change — a *quiet hour*, not lost capture. Same for the new **2026-08-21 esports h11-12**. `coverage()` shows the **only true gaps (no book at all) are exactly the three known events, both universes**: 06-19 h00-11 (capture start), the 06-22 15:00→06-23 08:00 OOM outage, and 08-21 h21-23 (reboot tail). Politics has **zero** unexplained gaps; esports has 24 quiet-hours across 14 days.
- **The Dem-nominee-2028 NegRisk event.** [CORRECTED IN STEP I] The 0.982 figure quoted here was a **sum of per-candidate time-medians, which is an invalid measure** — sum-to-1 holds only *instantaneously* and candidates live in different windows. Recomputed at a common timestamp (Step I): instantaneous YES-sum median 0.982 for this event. The general finding stands (NegRisk sums are worth chasing) but the method was wrong; see `reports/stepI.md` for the corrected cross-event distribution.
- **[CORRECTED] No NegRisk panel existed in Step H** — this report wrongly implied one ("its own dashboard view", "where this gets chased"). `load_event()` was built but the panel was not. It is built in Step I (I4).
- **The stale-book cohort. [CORRECTED]** The figure "21,522 esports tokens" is wrong — that is **both universes combined**. Correct: esports `near_half` = **21,082**, politics = **440**. Of the esports ones, **20,764** had their last observation within ±1h of settlement (196 stopped early, 122 have no `closed_time`). Still a large cohort of decided outcomes quoting ~0.5 — its own dashboard view is built in Step I.
- **Median spread is ~4¢** across tokens, but the distribution has a long tail to 100¢ (one-sided/locked books) — visible in the distributions panel.

## Stop conditions — none triggered
No E1 assertion failed after H0; the anti-drift test passed; every worked example wrote cleanly through the public API; both reconciliation identities still hold exactly (101,051,502 l1 / 7,227,528 trades); nothing surprising beyond the quiet-vs-gap clarification (documented). 

## Then STOP
`reports/stepH.md`, `STATUS.md`, `LOG.md` written. `book` (F) not started; nothing published to R2; nothing pushed. The operator reviews the dashboard and iterates on the UI before anything goes to the shared repo or the bucket.

**To run it:** `cd polymarket/research && .venv/Scripts/streamlit run dashboard/app.py`
