# Async networking — WebSocket client and HTTP session
aiohttp>=3.9.0
websockets>=12.0

# Environment loading
python-dotenv>=1.0.0

# Polymarket signing (required for live order submission)
py-clob-client>=0.18.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
